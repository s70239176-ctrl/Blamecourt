# Schema confirmed against the genlayer-test package's own PyPI docs
# (an earlier version of this file was an unverified guess -- this one
# matches https://pypi.org/project/genlayer-test/'s documented example).
networks:
  default: localnet

  localnet:
    url: "http://127.0.0.1:4000/api"
    leader_only: false

  studionet:
    # Pre-configured by genlayer-test -- accounts auto-generated

paths:
  contracts: "contracts"
  artifacts: "artifacts"

environment: .env
