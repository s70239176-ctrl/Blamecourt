# Confirmed against PyPI: the package is `genlayer-test` (it installs the
# `gltest` module/CLI -- `gltest` alone is not an installable package
# name; `pip install gltest` fails with "No matching distribution").
genlayer-test
pytest
